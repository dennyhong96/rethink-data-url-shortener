import styled from "styled-components";
import { motion } from "framer-motion";

export const StyledLinkContainer = styled(motion.div)`
	align-self: stretch;
	width: 100%;
	display: grid;
	gap: 1rem;
`;
